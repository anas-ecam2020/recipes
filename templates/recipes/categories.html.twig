{% extends 'base.html.twig' %}

{% block title %}Catégories
{% endblock %}


{% block body %}
{% for category in categories %}
		<br>
		<article>
			<h3>{{ category.title }}</h3>
			<div class="content">
				<img src="{{ category.image }}" alt=""><br><br>
			</div>
            <div>
            </div>
		</article>
		<br>
{% endfor %}
{% endblock %}