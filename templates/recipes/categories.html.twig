{% extends 'base.html.twig' %}

{% block title %}Catégories
{% endblock %}


{% block body %}
{% for category in categories %}
		<br>
		<article>
			<h3>{{ category.title }}</h3>
			<div class="content">
				<img src="{{ category.image }}" width="350" length="150" alt=""><br><br>
			</div>
            <div>
				<p>{{ category.content }}</p>
            </div>
		</article>
		<br>
{% endfor %}
{% endblock %}